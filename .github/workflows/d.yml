name: d

on:
    workflow_dispatch:
    push:

jobs:
    d:
        runs-on: ubuntu-latest
        steps:

            - run: |
                echo "long running cmd..."
                sleep 60s
                echo "done"
