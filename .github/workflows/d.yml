name: d

on:
    workflow_dispatch:
    push:

jobs:
    
    a:
      runs-on: ubuntu-latest
      steps:
        - name: Print contexts
          uses: ./.github/actions/print_contexts@${{ github.ref_name }}
        
        - run: ls -la
            
